{so_name, "gdal_nifs.so"}.

{port_envs, [
             %% Make sure to set -fPIC when compiling 
             {"CFLAGS", "$CFLAGS -std=c99"},
             {"LDFLAGS", "$LDFLAGS -lgdal"}
             ]}.



